---
description: 改 src 后必须用 Vite 打包回 userscript
alwaysApply: true
---

# 从 src 打包 userscript

油猴只能安装根目录的 `zhihu-enhancement-plus.user.js`。逻辑在 `src/*.ts`，样式在 `src/styles/*.css`，结巴 glue 在 `vendor/`。

## 怎么改

- 改功能编辑 `src/`，不要手改打包后的 `zhihu-enhancement-plus.user.js`
- 版本号改 `src/version.ts`（`vite.config.ts` 会读它写进油猴头）
- 改完执行 `npm run build`（Vite 单文件、不压缩，并复制到仓库根目录）
- 本地看设置/角标样式：`npm run preview:ui`
- 在知乎页热更新：`npm run dev`，浏览器安装带 `dev:` 前缀的脚本

## 不要

- 不要从页面再拉远程业务脚本；jieba WASM 继续走 `@resource`
- 不要打开 `build.minify`
