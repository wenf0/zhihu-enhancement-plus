---
description: 改 src 后必须用 WXT 构建 Chrome 扩展
alwaysApply: true
---

# 从 src 构建 Chrome 扩展

本仓库主产物是 Manifest V3 扩展，用 WXT 从 `src/` 构建。

## 怎么改

- 页面逻辑在 `src/lib/content/` 和 `src/lib/noise/`
- 设置页 / 弹层在 `src/entrypoints/options`、`src/entrypoints/popup`
- 版本号改 `src/lib/version.ts`（`wxt.config.ts` 会写进 manifest）
- 改完执行 `npm run build`，产物在 `.output/chrome-mv3/`
- 本地加载：Chrome → 扩展程序 → 开发者模式 → 加载已解压的扩展程序 → 选 `.output/chrome-mv3`

## 不要

- 不要手改 `.output/`
- 不要从页面再拉远程业务脚本；jieba WASM 走扩展包内 `public/jieba_rs_wasm_bg.wasm`
- 不要在 content script 里挂 React/shadcn（设置页可以）
- 不要再引入油猴 / userscript 单文件产物
