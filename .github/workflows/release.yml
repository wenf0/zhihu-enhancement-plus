name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Zip extension
        run: npm run zip

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: .output/zhihu-enhancement-plus-*-chrome.zip
          generate_release_notes: true
          fail_on_unmatched_files: true
